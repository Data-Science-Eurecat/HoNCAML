global:
  problem_type: classification

logging:
  level: DEBUG

steps:
  data:
    extract:
      filepath: data/processed/bank_test.csv
      target: class

  model:
    extract:
      filepath:
    transform:
      predict:
        path: reports
